# 結果: ヘルプをより詳しく(v1.1)

## 実施

- `resources/help/help_jp.md` / `help.md` を操作手順書に書き直した（起動、画面、状態と処置、区画の自動選択、ハードウェア/SMART/GPT、ボタン手順、ステータス文言、典型例 D:、よくある見間違い、CLI）。根拠は実装済みの画面文言と engine のエラーコードのみ。未実装の同一ディスク拒否は「拒まない」と明記。
- `md()` に番号リスト・箇条書きグループ・`code`・強調を追加。ヘルプ幅 880px。
- `dist/binary/QuickDiskRescue.exe` 再ビルド（1,835,740 bytes）。`HELP_MD.ja` 12,543 文字。`状態と処置` `ステータスに出る文言` `よくある見間違い` `no_valid_gpt_header` を確認。

## 検証

- バンドル HTML / `index_embed.html` に拡充本文あり。
- 番号リストが `<ol>` になるコードがバンドルに含まれる。
- ブラウザでのヘルプダイアログ目視は未実施（GUI 起動は UAC）。ヘルプ → ヘルプでスクロール確認が残る。

## 残課題

- 消失走査オフセットからのフォルダ救出、BitLocker OS パス、ドライブレター割り当て、同一ディスク保存の拒否は未実装のまま（ヘルプにもそう書いた）。
