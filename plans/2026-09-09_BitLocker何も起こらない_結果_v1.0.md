# 結果: BitLocker何も起こらない(v1.0)

診断完了と同時に BitLocker の全ツリー読みを始めていた。そのあいだクリックは busy で捨てられ、画面は止まって見えた。

- BitLocker は自動ツリーしない。行をクリックしたときだけ読む。
- クリック直後に「ツリーを読み込み中」を出す。
- ツリー JSON を深さ 2・400 ノードに制限。
- EXE 再ビルド済み。
