# 計画書: BitLocker何も起こらない(v1.0)

## 要望
BitLocker 行をクリックしても何も起こらない。

## 想定原因
診断直後の自動ツリーが C: 全体を読み、ワーカーが busy のまま。クリックは握りつぶされる。巨大 JSON で画面も止まる。失敗がステータスに出ない。

## 変更
- BitLocker は自動ツリーしない。クリックしたときだけ読む。
- クリック直後にステータスを出す。busy ならそれを出す。
- ツリー JSON を浅くする。
