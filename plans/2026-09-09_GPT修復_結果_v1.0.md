# 2026-09-09 GPT修復 結果

実装: CRC が通る GPT を正とし、実ディスクサイズに合わせて LastUsableLBA とバックアップ GPT を末尾へ書き直す。Protective MBR も合わせる。ファイル内容は触らない。

検証: `gpt_mismatch.img` で last_usable 3907029168 → 4062。修復後 `size_mismatch: false`。

実ディスクは JS confirm のあと MessageBox 2回。先にイメージ推奨。
