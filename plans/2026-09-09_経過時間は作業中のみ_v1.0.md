# 2026-09-09 経過時間は作業中のみ

## 指摘

起動後も動き続ける経過表示に意味がない。
作業している時間だけ動いて、完了で止まる仕様に変更する。

## 対応

`startClock` は診断・消失走査・ツリー・イメージ・救出・カービング開始時のみ。
完了（結果またはエラー）で `clearInterval` し、その時点の秒数を残す。
診断のあと自動で tree する場合は時計をリセットせず、tree 完了まで動かす。
