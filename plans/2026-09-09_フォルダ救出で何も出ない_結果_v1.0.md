# 2026-09-09 フォルダ救出で何も出ない 結果

原因: 常に区画 0 を見ていた。C が入った GPT ディスクの 0 は EFI (FAT) なので NTFS ではないと判定し、ツリーも copy も空だった。エラーも画面に出していなかった。

対応:
- NTFS シグネチャがある一番大きい区画を使う
- 診断の区画行をクリックして切り替え可能
- tree/copy 失敗時はステータスに error を出す
- EXE 再ビルド済み。ビルドのため起動中の QuickDiskRescue.exe は終了した。
